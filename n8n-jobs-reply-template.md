# n8n Jobs first-contact reply template (OFFLINE DRAFT – DO NOT POST yet)
Status: draft 2026-09-10. Posting waits on email-inbox access (forum signup needs email verification).
Rules: reply ONLY to explicit "looking for n8n expert – paid" briefs; answer the brief first, link demo second; never DM uninvited; never copy-paste identical text twice (rewrite per brief).

## Qualification (ALL must be true in the brief, else skip)
- Intake/CRM/lead-routing/automation pain stated (not generic "need developer")
- CRM named (HubSpot/Pipedrive/GHL/Sheets) or "help us choose" with volume given
- Budget or paid intent explicit
- Fewer than ~8 visible replies already

## Reply skeleton (<150 words, rewrite per brief)
```
Hi [name] – [one line proving I read the brief: their tool + their pain, e.g. "GHL pipeline with leads stalling after the web form"].

Closest thing I've shipped in the open: [pick ONE matching proof]
- intake demo (test data, runs in-browser): [demo URL]
- 6-node workflow file: [repo]/blob/main/workflow.json
- 12-point leak checklist (scores this exact problem 0–24): [gist URL]

Fixed-price only: audit $750 (credited to build), single flow $998, multi-channel + AI scoring $2.4–$4.5k. Done= checklist signed before work; fails at handover demo → free fix or 50% refund.

One scoping question: [sharp question from THEIR brief, e.g. "is GHL or Sheets the dedupe source of truth?"]

No retainer required. Happy to start with the audit so you see the ROI math before committing to a build.
```

## What NEVER goes in a reply
- Fake clients, history, testimonials, "we"/team language (solo), urgency tricks, off-platform contact asks (against most forum/marketplace rules – let THEM propose the channel).
- Rate-cutting to win ("$50 for everything") – protects positioning and JSS-equivalent reputation.

## Log each reply (worker.md)
`date | thread URL | brief fit (1–5) | reply sent? | response? | outcome`
Stop rule: 10 qualified replies → 0 conversations = reposition offer (price/niche/proof), report before continuing.
